{
  "name": "lighthouse-package",
  "version": "1.0.0",
  "description": "패키지 방식 lighthouse",
  "main": "src/index.js",
  "scripts": {
    "test": "node src/test.js",
    "install-deps": "npm install"
  },
  "dependencies": {
    "lighthouse": "^9.6.8",
    "chrome-launcher": "^0.15.2",
    "fs-extra": "^11.1.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}